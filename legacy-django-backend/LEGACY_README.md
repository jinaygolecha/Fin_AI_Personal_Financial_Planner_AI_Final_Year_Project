# ARCHIVED: Legacy Django Backend

This directory contains the original Python/Django prototype for the Financial AI project.
The project has been fully re-engineered and consolidated into the Node.js/Express + Prisma + PostgreSQL stack located in `node-backend/` and `frontend/public/`.

## Status: ARCHIVED
- Do not run Django servers from this folder in production.
- All core banking, transaction, AI financial twin, Gemini integrations, and live market data feeds are actively maintained in `node-backend/`.
